[package]
name = "data-collector"
version = "0.1.0"
edition = "2018"

[dependencies]
lambda_runtime = "0.2"
lambda_http = "0.1"
http = "0.1"
kiln_lib = { git = "https://github.com/simplybusiness/Kiln" }
serde_json = "2.0"
serde = { version = "1.0", features = ["derive"] }
