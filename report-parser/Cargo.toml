[package]
name = "report-parser"
version = "0.1.0"
edition = "2018"

[dependencies]
avro-rs = "0.6"
failure = "0.1"
kiln_lib = { git = "https://github.com/simplybusiness/Kiln", branch = "master", features = ["avro", "streaming"] }
kafka = "0.8"
regex = "1"
